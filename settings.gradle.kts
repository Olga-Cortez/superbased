pluginManagement {
	repositories {
		google()
		gradlePluginPortal()
		mavenCentral()
	}
}

rootProject.name = "superbased-ide"

// Single-module project. This file exists only so IDE/Gradle tooling can
// recognize the workspace as a Gradle project (FAST uses its own build system).
